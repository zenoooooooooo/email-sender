export default async function getEmailHistory(user: string) {
    try {
        
    } catch (error) {
        console.error(error)
        return {
            message: "Internal Server Error",
            status: 500
        }
    }
}